import type { ArchitectureSummary } from "@codexel/shared";

export async function classifyArchitecture(workspacePath: string): Promise<ArchitectureSummary> {
  // Skeleton implementation for Phase 0 foundation
  return {
    layers: [],
    boundaries: [],
  };
}
